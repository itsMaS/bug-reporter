# Bundled rclone

Place the Windows rclone binary in this folder:

- rclone.exe

At publish time, everything under tools/rclone is copied into publish output.

Expected publish path:

- tools/rclone/rclone.exe
