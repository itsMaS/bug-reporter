# Bundled FFmpeg Assets

Place FFmpeg binaries for win-x64 in this folder structure:

- tools/ffmpeg/bin/ffmpeg.exe
- tools/ffmpeg/bin/ffprobe.exe

These files are copied into publish output by bug-reporter.csproj when present.

## Recommended tracking

Record the FFmpeg version and source URL you used for internal traceability.
